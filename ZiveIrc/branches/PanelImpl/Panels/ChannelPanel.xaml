<UserControl x:Class="Zive.Irc.WpfUi.ChannelPanel"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            mc:Ignorable="d"
            d:DesignHeight="300" d:DesignWidth="300"
            >
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <FlowDocumentScrollViewer Name="ScrollbackViewer" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Stretch" />
        <ListBox Name="NickList" Width="150" Grid.Row="0" Grid.Column="1" MinWidth="150" HorizontalAlignment="Right" BorderThickness="1,0,0,0" SelectionMode="Extended">

        </ListBox>

        <RichTextBox Name="TextInputArea" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" />

    </Grid>

</UserControl>
